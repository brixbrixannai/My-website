# My-First website
<header class="brand-header">
  <img 
    src="charitywater_logo_horizontal_BlackText.png" 
    alt="charity: water logo"
    class="logo"
  />
</header>
.brand-header {
  display: flex;
  justify-content: center;
  padding: 20px 0 10px;
}

.logo {
  max-width: 260px;
  height: auto;
}
div.textContent = tile.special ? tile.special[0].toUpperCase() : "";
if (tile.special === "source") {
  div.innerHTML = `<img src="jerry_can_yellow.png" class="icon" />`;
}
if (tile.special === "filter") {
  div.innerHTML = `<span class="filter-label">Filter</span>`;
}
if (tile.special === "tap") {
  div.innerHTML = `<img src="jerry_can_black.png" class="icon" />`;
}
.icon {
  width: 36px;
  height: 36px;
  pointer-events: none;
}

.filter-label {
  font-size: 12px;
  font-weight: 600;
}
alert("You win! Clean water delivered 💧");
alert(
  "Clean water delivered 💧\n\n" +
  "771 million people still lack access to clean water.\n" +
  "charity: water funds real solutions."
);
<footer class="credit">
  Built to support charity: water’s mission
</footer>
.credit {
  margin-top: 30px;
  font-size: 12px;
  color: #555;
}
